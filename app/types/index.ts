export interface SliderItem {
  id: number;
  icon: string;
  title: string;
  desc: string;
}

export interface FeatureItem {
  id: number;
  icon: string;
  colorClass: string;
  title: string;
  desc: string;
}
